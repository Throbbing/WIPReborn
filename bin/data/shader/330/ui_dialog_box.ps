#version 330 core
out vec4 frag_color;

in vec4 out_position;
in vec2 out_texcoord;

uniform sampler2D in_texture;
uniform vec4 in_color;

void main()
{
    float u = out_texcoord.x;
    float v = out_texcoord.y;
    float a = 0.5*0.25;
    //if(u<a)
	   //frag_color = texture(in_texture, out_texcoord);

	frag_color = in_color;
	//if(u<0.02||u>0.98||v<0.02||v>0.98)
	//frag_color = vec4(0.1,0.1,0.1,0.8);
}